module.exports = {};

// var Schema = require('jugglingdb').Schema;

// exports.init = function (server, next) {

//     var Snack = server.app;
//     var config = Snack.config();

//     exports.schema = new Schema(config.db.engine, config.db);
//     exports.server = server;

//     next();
// };
